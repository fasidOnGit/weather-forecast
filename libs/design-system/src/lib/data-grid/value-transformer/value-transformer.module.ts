import { NgModule } from '@angular/core';
import { ValueTransformerPipe } from './value-transformer.pipe';

/**
 * Value transformer pipe.
 */
/**
 * The value transformer pipe.
 */
@NgModule({
	declarations: [ValueTransformerPipe],
	exports: [ValueTransformerPipe],
})
export class ValueTransformerModule {}
